
import { FC } from "react";

const MealDetails: FC = () => {
  // Extract the dynamic route parameter using useRouter
 // Accessing the "mealId" dynamic segment

  return <h1>Hi Meals Page Ishan</h1>;
};

export default MealDetails;
